// import 'package:botybuy/pages/home_page/home_page.dart';
// import 'package:flutter/material.dart';

// getOnGenerateRoutes(settings) {
//   Widget page;
//  if (settings.name == 'home') {
//    page = HomePage();
//    }
// //   else if (settings.name == routeSettings) {
// //    page = SettingsScreen();
// //  } else if (settings.name!.startsWith(routePrefixDeviceSetup)) {
// //    final subRoute = settings.name!.substring(
// //      routePrefixDeviceSetup.length,
// //    );
// //    page = SetupFlow(
// //      setupPageRoute: subRoute,
// //    );
// //  } 
//  else {
//    throw Exception('Unknown route: ${settings.name}');
//  }
// final  page=MaterialPageRoute(builder: builder)<dynamic>(
//    builder: (context) {
//      return page;
//    };
//  return,
//    settings: settings,
//  );
